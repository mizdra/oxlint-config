import base from "./base.json" with { type: "json" };
import typescript from "./typescript.json" with { type: "json" };
import node from "./node.json" with { type: "json" };
import react from "./react.json" with { type: "json" };

const mizdra = {
  base,
  typescript,
  node,
  react,
};

export default mizdra;
